name: Ladybird GTK Arch

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  build:
    runs-on: ubuntu-latest

    container:
      image: archlinux:latest

    env:
      CCACHE_DIR: /tmp/ccache
      BUILD_PRESET: Release

    steps:
      - name: Update Arch
        run: |
          pacman -Syu --noconfirm

      - name: Install Dependencies
        run: |
          pacman -S --needed --noconfirm \
            autoconf-archive \
            base-devel \
            bison \
            ccache \
            cmake \
            curl \
            git \
            less \
            libgl \
            libpulse \
            nasm \
            ninja \
            python \
            qt6-base \
            qt6-tools \
            rust \
            sudo \
            tar \
            unzip \
            zip \
            vulkan-headers \
            vulkan-icd-loader

      - name: Checkout Repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          submodules: recursive

      - name: Mark Workspace Safe
        run: |
          git config --global --add safe.directory "$GITHUB_WORKSPACE"

      - name: Create Builder User
        run: |
          useradd -m -s /bin/bash builder

          chown -R builder:builder "$GITHUB_WORKSPACE"

          mkdir -p /tmp/ccache
          chown -R builder:builder /tmp/ccache

          echo "builder ALL=(ALL) NOPASSWD:ALL" >> /etc/sudoers

      - name: Restore ccache
        uses: actions/cache@v4
        with:
          path: /tmp/ccache
          key: ccache-${{ runner.os }}-${{ github.ref_name }}
          restore-keys: |
            ccache-${{ runner.os }}-

      - name: Configure ccache
        run: |
          ccache --set-config=max_size=5G
          ccache --zero-stats

      - name: Build Ladybird GTK
        run: |
          sudo -u builder bash -c "
            cd '$GITHUB_WORKSPACE'
            git config --global --add safe.directory '$GITHUB_WORKSPACE'
            python Meta/ladybird.py build --gui Gtk
          "

      - name: Show Build Output
        run: |
          find Build/release -type f | sort

      - name: Find Lagom Libraries
        run: |
          find Build -name 'liblagom*.so*' || true
          find Build -name '*.so*' | head -200 || true

      - name: Package Build
        run: |
          mkdir -p package

          cp -a Build/release/. package/

          if [ -d Base/res ]; then
            mkdir -p package/Base
            cp -a Base/res package/Base/
          fi

          tar -czf ladybird-gtk-arch.tar.gz package

      - name: Upload Artifact
        uses: actions/upload-artifact@v4
        with:
          name: ladybird-gtk-arch
          path: ladybird-gtk-arch.tar.gz
          retention-days: 30
